# The main(parent) values file.

global:
  postgres:
    enabled: true
  api-chart: 
    enabled: true
  ingress:
    enabled: true

api-chart:
  image:
    tag: v0.1

  configMap:
    mailServer: smtp.gmail.com
    mailPort: 465
    mailUseTLS: false
    mailUseSSL: true
    appSettings: development
  
  secret:
    databaseURL: postgres:5432/bright_events
    secretKey: 'change-this'
    securityPasswordSalt: 'change-this' 
    securityPasswordResetSalt: 'change-salt'
    appMailUsername: 'mail-username'
    appMailPassword: 'mail-passwrd'
    mailDefaultSender: 'sammysteppa90@gmail.com'
  
  autoscaling:
    enabled: false
    minReplicas: 1
    maxReplicas: 15
    targetCPUUtilizationPercentage: 80
    # targetMemoryUtilizationPercentage: 80